name: Deploy to Beget

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Получаем код из репозитория
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Настраиваем SSH-доступ
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.BEGET_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          echo -e "Host *\n\tStrictHostKeyChecking no\n" > ~/.ssh/config

      # 3️⃣ Отладка: выводим хост
      - name: Show target host
        run: echo "Deploying to ${{ secrets.BEGET_HOST }}"

      # 4️⃣ Очищаем и создаём папку на сервере
      - name: Prepare remote directory
        run: |
          ssh -i ~/.ssh/id_rsa root@${{ secrets.BEGET_HOST }} "
            rm -rf /root/app &&
            mkdir -p /root/app
          "

      # 5️⃣ Копируем проект через rsync (надёжнее, чем scp)
      - name: Sync project to Beget via rsync
        run: |
          rsync -avz --delete \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='.github' \
            -e "ssh -i ~/.ssh/id_rsa" ./ root@${{ secrets.BEGET_HOST }}:/root/app

      # 6️⃣ Собираем и запускаем контейнер на сервере
      - name: Build and run Docker on Beget
        run: |
          ssh -i ~/.ssh/id_rsa root@${{ secrets.BEGET_HOST }} "
            docker system prune -af --volumes &&
            cd /root/app &&
            docker compose build --no-cache &&
            docker compose up -d
          "

      # 7️⃣ Финальный шаг
      - name: ✅ Deployment complete
        run: echo "🎉 Успешный деплой на Beget завершён!"
