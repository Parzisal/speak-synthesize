name: Deploy to Beget

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Получаем код из репозитория
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Настраиваем SSH-доступ
      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.BEGET_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          echo -e "Host *\n\tStrictHostKeyChecking no\n" > ~/.ssh/config

      # 3️⃣ Отладка: выводим хост
      - name: Show target host
        run: echo "Deploying to ${{ secrets.BEGET_HOST }}"

      # 4️⃣ Обновляем репозиторий на сервере
      - name: Git pull on Beget
        run: |
          ssh -i ~/.ssh/id_rsa root@${{ secrets.BEGET_HOST }} "
            if [ ! -d /root/app/.git ]; then
              git clone --depth=1 -b main git@github.com:Parzisal/speak-synthesize /root/app
            else
              cd /root/app &&
              git reset --hard &&
              git pull origin main
            fi
          "

      # 5️⃣ Собираем и запускаем контейнер на сервере
      - name: Build and run Docker on Beget
        run: |
          ssh -i ~/.ssh/id_rsa root@${{ secrets.BEGET_HOST }} "
            docker system prune -af --volumes &&
            cd /root/app &&
            docker compose build --no-cache &&
            docker compose up -d
          "

      # 6️⃣ Финальный шаг
      - name: ✅ Deployment complete
        run: echo "🎉 Успешный деплой через git pull завершён!"
